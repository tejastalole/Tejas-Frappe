[project]
name = "bulk_data_tools"
authors = [
    { name = "Tejas", email = "hello@tejas.com"}
]
description = "Bulk delete and manage DocType records in Frappe / ERPNext"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"
