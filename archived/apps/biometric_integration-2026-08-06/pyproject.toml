[project]
name = "biometric_integration"
authors = [
    { name = "Tejas", email = "hello@tejas.com"}
]
description = "ADMS / ZKTeco biometric device integration for Frappe / ERPNext"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.dev-dependencies]

[deploy.dependencies.apt]
packages = []
