[project]
name = "zkteco_integration"
authors = [
    { name = "Exacuer", email = "support@exacuer.com"}
]
description = "Exacuer Biometric — ZKTeco local agent to ERPNext Cloud Employee Checkin"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.dev-dependencies]

[deploy.dependencies.apt]
packages = []
